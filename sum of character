#include <stdio.h>

Void main() {
    char str[100];
    int sum = 0, i = 0;
    printf("Enter a string: ");
    gets(str);
    while (str[i] != '\0') {
        sum += str[i];
        i++;
    }
    printf("The sum of ASCII values of the characters is: %d\n", sum);

    return 0;
}
